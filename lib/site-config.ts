// ============================================================================
// Site-wide launch switch.
// ============================================================================
// The main karatrack.com product site is NOT public yet (beta testing).
// While this is false, the Search Engine hides every link that would lead
// visitors to the main site: header nav (Products/Pricing/Roadmap/Updates/
// Support), the Login pill, and the footer Contact/Support links.
//
// AT GO-LIVE: change `false` to `true` below, then push. That's the whole
// step — every hidden link reappears at once.
// ============================================================================

export const MAIN_SITE_LIVE = false;
